version: '3.8'

services:
  weather-scraper:
    image: weather-scraper
    build: .
    container_name: lv-weather
    restart: unless-stopped
    networks:
      - weather-net
    volumes:
      - ./output:/app/output  # Only needed if you want to save screenshots

networks:
  weather-net:
    driver: bridge